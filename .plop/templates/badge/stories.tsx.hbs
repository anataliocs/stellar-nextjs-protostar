import type { Meta, StoryObj } from '@storybook/react';

import { {{pascalCase variant}} } from './{{pascalCase variant}}';

const meta: Meta
<typeof {{pascalCase variant}}/> = {
    title: 'Badge/{{pascalCase variant}}',
    component: {{pascalCase variant}},
    tags: ['autodocs'],
    args: {
    children: 'Example',
    }
    };

    export default meta;

    type Story = StoryObj
    <typeof {{pascalCase variant}}/>;

export const Default: Story = {};
